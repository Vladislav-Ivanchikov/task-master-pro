{
  "name": "taskmaster",
  "private": "true",
  "scripts": {
    "build:ui-kit": "pnpm --filter @taskmaster/ui-kit build",
    "build:client": "pnpm --filter taskmaster-client build",
    "build": "pnpm run build:ui-kit && pnpm run build:client"
  }
}
